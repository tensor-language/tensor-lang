//@test(expect = "value=20")
namespace main

extern c {
    func printf(*byte, ...) int32
}

func main() int32 {

    let arr = alloca<int32>(5)
    arr[0] = 10
    arr[1] = 20
    let ptr: *int32 = arr
    let next = ptr + 1
    printf("value=%d\n", *next)

    return 0
}