//@test(expect = "x=20 y=10")
namespace main

extern c {
    func printf(*byte, ...) int32
}

func swap<T>(a: *T, b: *T) {
    let tmp: T = *a
    *a = *b
    *b = tmp
}

func main() int32 {

    let x = 10
    let y = 20
    swap<int32>(&x, &y)
    printf("x=%d y=%d\n", x, y)

    return 0
}